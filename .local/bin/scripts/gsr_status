#!/usr/bin/env sh

if pidof gpu-screen-recorder >/dev/null; then
  echo '{"text": "󰻂", "class": "recording", "tooltip": " Recording in progress" }'
else
  echo '{"text": "", "class": "idle", "tooltip": ""}'
fi
